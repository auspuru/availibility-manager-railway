{
  "name": "availability-manager",
  "version": "1.0.0",
  "description": "Collaborative scheduling platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "dev": "node server.js",
    "railway": "npm run build && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "socket.io": "^4.6.1",
    "uuid": "^9.0.0"
  }
}
